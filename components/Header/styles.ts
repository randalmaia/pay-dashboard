import styled from "styled-components";

const Header = styled.header`
  display: flex;
  align-items: center;
  width: 100%;
  padding: 30px 0;

  h1 {
    margin-right: 60px;
  }
`;

export default { Header };
